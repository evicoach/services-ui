import React from "react";
import classes from "./Footer.module.css";

const footer = (props) => (
    <footer className={classes.Footer}>
        <p>This is the footer of the service page</p>
    </footer>
);

export default footer;